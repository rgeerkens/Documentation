name: Changelog

on:
  #push:
  #  branches:
  #    - master
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@master
        with:
          fetch-depth: 0
      - name: Build Changelog
        id: github_release
        uses: mikepenz/release-changelog-builder-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          commitMode: true
      - name: Create local changes
        run: |
          echo ${{steps.build_changelog.outputs.changelog}} >> changelog.md
          log=${{steps.build_changelog.outputs.changelog}}
          echo "test"
          echo log
          echo ${{steps.build_changelog}} 
          echo "testclose"
#          git add changelog.md
#      - name: Commit files
#        run: |
#          git config --local user.email "ronny.geerkens@telenet.be"
#          git config --local user.name "rgeerkens"
#          git commit -am "Change Log Created / Updated"
#      - name: Push changes
#        uses: ad-m/github-push-action@master
#        with:
#          github_token: ${{ secrets.GITHUB_TOKEN }}
#          branch: ${{ github.ref }}
